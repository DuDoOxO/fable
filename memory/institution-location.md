---
name: institution-location
description: 全域工作制度檔案的位置與用途（2026-07-05 由 Fable 5 建立）
metadata: 
  node_type: memory
  type: project
  originSessionId: c0bb0342-aa2a-4ab6-86f5-440a85c7d9fc
---

2026-07-05 的 Fable 5 session 在 `~/.claude/` 建立了跨專案的工作制度：`~/.claude/CLAUDE.md`（路由與硬規則，每 session 自動載入）+ `~/.claude/rules/` 六個細則檔（diagnosis、delegation、judgment、prompts、maintenance、letter）。

**Why:** 使用者只有一次使用 Fable 5 的機會，指定把強模型判斷力外化成弱模型可執行的制度。

**How to apply:** 這些檔案的維護規則在 `~/.claude/rules/maintenance.md`——改之前先讀它（備份、權限分級、行數上限）。不要在本 memory 目錄重複制度內容，單一事實來源在 `~/.claude/rules/`。
